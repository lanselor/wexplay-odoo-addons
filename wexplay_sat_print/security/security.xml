<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    
    <!-- Grupo técnico para configuración de impresión SAT -->
    <record id="group_wexplay_sat_print_admin" model="res.groups">
        <field name="name">Wexplay SAT Print Admin</field>
        <field name="category_id" ref="base.module_category_hidden"/>
        <!-- Implica administrador del sistema -->
        <field name="implied_ids" eval="[(4, ref('base.group_system'))]"/>
    </record>
    
</odoo>
