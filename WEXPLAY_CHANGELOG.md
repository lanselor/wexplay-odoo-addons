# Wexplay Changelog

## 2026-05-10

### wex_teardown

- Añadido bloque visible `Reacondicionamiento` en producto para productos de despiece.
- Añadidos campos de huella de despiece en `product.template`: componente, part number y modelo SAT.
- Limitada la búsqueda de coincidencias a productos reacondicionados.
- Añadido botón `Buscar coincidencias` al flujo de piezas.
- Añadidos niveles de coincidencia y color visual en listas:
  `exact`, `partial`, `model`, `none`.
- Mejorada la heurística local de coincidencias con normalización ligera de nombres y soporte mejorado para diferencias pequeñas en el modelo.
- Documentado el enfoque escalable de matcher interno ahora y servicio externo futuro.
- Replanteada la pestaña `Piezas` como fase de revisión física.
- Añadidas dos listas en piezas: piezas pendientes/aptas y piezas no aptas o no recuperadas.
- Eliminada la edición inline de piezas en el flujo principal; la edición completa pasa al formulario de línea.
- Movida la selección de plantilla a la pestaña `Dispositivo`.
- Separada la decisión de producto (`crear nuevo`, `usar existente`, `descartar`) hacia la fase de coincidencias.
- `list_price` queda como precio principal sin IVA; `pvp_tax_included` pasa a auxiliar calculado con impuestos nativos de Odoo.
- Se evita crear configuración fiscal paralela para despieces.
- El control de calidad mueve líneas no aptas/no recuperadas fuera del flujo normal sin borrarlas.
- Traducidas etiquetas visibles principales de las vistas del módulo al castellano.
- Actualizada la arquitectura del módulo con las nuevas decisiones de flujo.

## 2026-05-26

| Fecha | Módulo(s) modificados | Descripción larga de los cambios | Decisiones de negocio |
|---|---|---|---|
| 2026-08-23 | wexplay_repair, wexplay_portal, wexplay_portal_repair_reports, wexplay_portal_repair_workflow | Se consolidó la gestión documental del informe técnico SAT. En backend se rediseñó el informe QWeb para priorizar diagnóstico, intervención, material y evidencia fotográfica, sin importes ni totales; se limitaron las fotos a dos por hoja con marco, se añadieron notas complementarias regenerables y una acción `Enviar` que abre el compositor nativo de Odoo con el PDF archivado en DMS como adjunto temporal y la nueva plantilla `SAT - Informe técnico` precargada. En portal se creó el módulo de informes bajo demanda con variantes Wexplay y personalizada por empresa, perfil de identidad corporativa, logotipo, color validado, formulario de vista previa del logo y generación directa sin persistir el PDF. También se añadió indicador de carga, prevención de doble pulsación, evento de descarga, chatter interno y métrica/actividad en Portal clientes. | Se decide que el informe sea un documento técnico y no económico: precios, impuestos y totales permanecen en presupuestos, proformas y facturas. El informe interno se conserva en DMS como documento vigente; el portal genera PDFs efímeros bajo demanda. Los clientes B2B no pueden cambiar texto técnico, notas ni fotografías: ambos formatos incluyen todas las imágenes ya autorizadas en portal y excluyen vídeos. La variante personalizada no revela Wexplay, referencias SAT, estados, personal ni consentimientos. Las descargas se trazan solo después de renderizar correctamente, usando el chatter nativo y la infraestructura de actividad existente, sin crear un sistema paralelo. |
| 2026-08-07 | wex_print_core | Se consolidó el alta guiada de impresoras locales detectadas por QZ Tray. Desde la búsqueda se abre un asistente que toma el nombre real de QZ y permite asociar los tipos documentales compatibles; al confirmar crea el dispositivo, reutiliza o crea un perfil estándar y genera las asignaciones necesarias sin duplicar ni dispositivos ni reglas equivalentes. La ficha de dispositivo incorpora `Añadir documentos`, muestra las asignaciones efectivas y deriva sus reportes y formatos directamente de ellas. Las capacidades se sincronizan al crear, editar o eliminar asignaciones y al actualizar sus perfiles. Se añadieron permisos y pruebas transaccionales para el alta inicial y la ampliación de un dispositivo existente. | Se decide que perfiles y asignaciones son la fuente de verdad documental de una impresora; los reportes y formatos visibles son informativos y no se mantienen manualmente en un tercer lugar. El asistente nunca cambia la configuración Legacy y deja desactivada por defecto la resolución nueva, para que toda impresora pase primero por validación operativa. Una impresora ya registrada se amplía con documentos sin generar otro dispositivo. |
| 2026-08-02 | wex_purchase_list | Se pulió la interfaz de la vista operativa de Reservas. La acción individual de WhatsApp adopta una apariencia asociada al canal, con borde y estado de interacción verdes, icono y texto correctamente separados y foco accesible. Se retiraron a la vez los checkboxes, la selección total y la barra de acciones masivas de Reservas: ese tablero no dispone de una operación masiva válida y el botón de crear orden de compra podía aparecer sobre líneas que por estado no pueden generar RFQ. La selección y creación agrupada de RFQ se mantienen exclusivamente en la lista de compra general. | Se decide que Reservas es un flujo de seguimiento y comunicación individual, no un punto de creación de pedidos de compra. La UI no debe presentar acciones que el backend rechazará por estado. WhatsApp debe ser reconocible visualmente sin dejar de integrarse con los controles nativos de Odoo. |
| 2026-08-02 | wex_purchase_list | Se añadió una política de reserva por cotización comercial para resolver el caso de equipos a medida con numerosos componentes. El nuevo toggle `Marcar como reserva` aparece junto a los términos de pago, está activo por defecto y determina si los productos añadidos desde esa venta deben entrar como reservas con aviso posterior al cliente. Al modificarlo en borrador o cotización enviada, se sincronizan las líneas de compra vinculadas que todavía no han sido avisadas, incluyendo las recibidas pendientes de comunicación. Las cotizaciones vinculadas a SAT quedan excluidas de esta opción y mantienen su flujo separado de piezas de reparación. | Se decide que una venta comercial se trata como reserva por defecto, pero un equipo a medida puede desactivar la política de toda la cotización. Se acepta abrir otra venta en el caso infrecuente de mezclar un montaje interno con un producto que sí requiere aviso. SAT conserva como fuente de verdad `stock.move.wex_is_reservation`; no se permite que una cotización vinculada a reparación altere esa decisión. Los avisos históricos nunca se revierten. |
| 2026-08-02 | wex_purchase_list | Se rediseñó la entrada operativa de Reservas mediante una vista XML propia que reutiliza el renderizador de la lista de compra, pero separa su UX. Las agrupaciones verticales se sustituyen por un HERO horizontal de cuatro paneles: `Con retraso`, `Esperando llegada`, `Pendientes de aviso` y `Ya avisadas`; cada panel muestra su contador y carga solo las reservas de ese bloque. La fila de Reservas incorpora además acciones rápidas: WhatsApp cuando existe cliente y marcado de aviso cuando la reserva ya fue recibida y aún no está notificada. Tras marcarla, el tablero se recarga para reflejar su traslado a `Ya avisadas`. | Se decide tratar `Con retraso` como panel propio y prioritario, no ocultarlo dentro de pendientes de aviso. Se confirma que el aviso al cliente es trabajo operativo de la lista de Reservas y no debe requerir abrir la ficha. La lógica de permisos y validación continúa centralizada en Python; la vista solo expone acciones cuando sus precondiciones son visibles. |
| 2026-08-02 | wex_purchase_list | Tras validar que el navegador no permite abrir de forma fiable varias URLs desde una única acción, se retiró el botón de apertura masiva y toda su lógica JavaScript. La vista conserva la apertura individual por línea, pero la columna `Enlace` se amplía y el control `Abrir` pasa a ocupar prácticamente toda la celda con mayor altura, contraste y estado hover. Cuando no hay URL configurada se muestra `Sin enlace` de forma explícita. También se ajustó la persistencia de selección: se conserva solo como puente temporal al volver desde una ficha, se consume en esa restauración, caduca a los cinco minutos y se elimina al recargar la página. | Se decide no prometer una apertura múltiple que depende de políticas externas del navegador. La URL individual sigue siendo funcionalidad real de la lista y debe ser rápida de pulsar sin alterar la apertura directa de la ficha. La selección debe proteger frente a un clic accidental, pero nunca convertirse en estado permanente ni sobrevivir a una recarga manual. |
| 2026-08-02 | wex_purchase_list | Se sustituyó la apertura secuencial de enlaces de proveedor por un último intento de apertura múltiple compatible con el gesto original de compra. El botón operativo pasa a intentar abrir, desde una única pulsación, una pestaña por cada URL de las líneas seleccionadas. Para evitar dependencias de colas, esperas o llamadas asíncronas que rompan el gesto del usuario, el renderizador genera enlaces HTML nativos con `target="_blank"` y los activa de forma inmediata durante el mismo evento de clic. El resultado debe verificarse con las ventanas emergentes permitidas para el sitio local, porque el navegador mantiene el control final sobre cuántas pestañas autoriza. | Se decide priorizar una última prueba de apertura masiva porque refleja mejor la operativa diaria de comprar varios repuestos. La solución no promete saltarse las políticas de seguridad del navegador: si este mecanismo sigue bloqueando enlaces, se abandonará la apertura automática múltiple y se elegirá una interacción explícita por enlace. |
| 2026-08-02 | wex_purchase_list | Se mejoró la operativa de selección en la vista personalizada de lista de compra. La columna inicial deja de depender de un checkbox pequeño: toda la franja izquierda de cada línea permite seleccionar o retirar la selección, mientras que el resto de la fila mantiene su clic habitual para abrir la ficha y editarla. La barra operativa incorpora selección total sobre las líneas visibles del filtro actual, deselección rápida y persistencia temporal de la selección al abrir una ficha y regresar. Además, la apertura de URLs de proveedor pasa a ser secuencial: cada pulsación abre una pestaña y muestra cuántos enlaces quedan, evitando que el navegador bloquee una apertura múltiple masiva. | Se decide preservar la edición directa mediante clic sobre la fila, porque es parte de la velocidad de trabajo diaria. La selección debe ser visible, amplia y acotada a las líneas presentes en el filtro actual. Se acepta una cola de enlaces, en vez de intentar abrir todas las URLs simultáneamente, para que el flujo sea fiable con la protección anti-popups de los navegadores. |
| 2026-06-13 | wex_purchase_list | Se continuó la iteración de la lista de compra interna para reforzar su uso como vista operativa diaria y no solo como tabla administrativa. La vista custom `wex_operational_list` se consolidó como entrada principal para compras y reservas, manteniendo selección múltiple, apertura rápida de enlaces de proveedor y agrupación visual por bloques. Además, se terminó de cablear la acción de marcado de cliente avisado desde la propia vista operativa para que la gestión de reservas recibidas no obligue a entrar ficha por ficha. También se retocó el SCSS de grupos y bloques para hacer más legible la jerarquía visual del tablero y separar mejor estados, agrupaciones y recuentos sin cambiar las reglas de negocio subyacentes del módulo. | Se decide que `wex_purchase_list` debe seguir evolucionando como herramienta operativa interna inspirada en la antigua hoja de trabajo del equipo, priorizando rapidez de uso y señales visuales claras. Se confirma que marcar al cliente como avisado debe poder hacerse desde la vista operativa cuando el repuesto ya está recibido, sin obligar a pasar por formularios intermedios. La mejora se considera UX operativa, no cambio de flujo de compra. |
| 2026-06-07 | wex_vendor_bill_ocr | Se abrió una nueva línea de trabajo para automatizar la recepción administrativa de compras mediante un módulo propio de OCR ligero de facturas PDF de proveedor vinculado a `purchase.order`. La primera versión incorpora subida manual del PDF desde la compra, creación de un job persistente con secuencia propia, cola procesada por `cron`, extracción de texto directa con fallback OCR cuando haga falta, parseo de proveedor, referencia, fecha e importes, revisión humana previa y aplicación final sobre el flujo nativo: validación de recepciones pendientes, captura de seriales cuando el producto lo exige, creación de la factura de proveedor desde la compra y copiado del PDF al `account.move` resultante. El diseño deja trazabilidad completa del proceso y no oculta dependencias técnicas como `pypdf`, `pytesseract` o `pdf2image`. | Se decide no intentar una automatización “ciega”: el OCR prepara datos, pero la validación final sigue pasando por un wizard de revisión antes de recibir y facturar. Se confirma que la fuente de verdad documental debe seguir siendo la `purchase.order` y su `vendor bill` nativa, evitando modelos paralelos de factura. También se decide que la fase inicial solo soporta PDFs y que la cola debe persistir estado, progreso, errores y reintentos de forma explícita para mantener trazabilidad operativa. |
| 2026-05-26 | wexplay_repair | Se abrió una fase experimental de mejora operativa de SAT alrededor de `repair_card_v2`, manteniendo `repair_card` como referencia estable. En esta iteración se introdujo un hero superior para navegar por buckets reales del workflow SAT, filtros operativos como `Mis órdenes` y `Sin responsable`, ampliación del catálogo cerrado de `Prioridad SAT`, plazos configurables por prioridad desde ajustes y una primera generación de alertas laterales calculadas con `create_date + prioridad` en lugar de `schedule_date`. También se hizo que hero, listado y panel lateral compartan el mismo dominio activo para que el técnico vea una única realidad filtrada. | Se decide que el hero superior se basa en estados y ubicaciones reales del flujo SAT, no en `Prioridad SAT`. Se decide que `Prioridad SAT` pasa a ser el eje temporal y operativo con catálogo cerrado (`Normal`, `Urgente`, `Empresa`, `Garantía`, `Presupuesto`, `Presupuesto 2`, `Express`) y plazos configurables en ajustes. Se descarta `schedule_date` como fuente de retraso para esta fase. Se fija que la acción SAT debe abrir con `Mis órdenes` y agrupación por fecha, y que hero, listado y sidebar deben recalcularse siempre sobre el mismo dominio activo. |
